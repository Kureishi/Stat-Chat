# Core data processing
pandas>=2.0
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3

# File format support
openpyxl>=3.1      # Excel read/write (.xlsx)
xlrd>=2.0          # Excel read (.xls legacy)

# PDF report generation
reportlab>=4.0
matplotlib>=3.7

# Image handling (app icon + vision input)
Pillow>=9.0

# LLM backend (Claude API + LM Studio)
requests>=2.28

# Note: tkinter ships with Python — no pip install needed.
# Linux users may need: sudo apt install python3-tk
